# 智连词境 (AI Word Connect Game)

一个基于 AI 内容生成的网页端词语连连看学习游戏，通过游戏化机制提升词汇理解与记忆能力。

## 功能特点

- 🎮 经典连连看玩法（≤2 拐点消除）
- 🌐 支持中英文双语模式
- 📊 三级难度系统（初级/中级/高级）
- 🤖 AI 动态生成词汇内容
- 📚 本地词库兜底，无需联网也能玩
- 🎯 智能难度调整

## 技术栈

- 前端：Phaser 3 + TypeScript + Vite
- 后端：Node.js + Express + TypeScript
- AI：大语言模型接口（可配置）

## 项目结构

```
├── client/          # 前端游戏客户端
├── server/          # 后端 API 服务
├── shared/          # 共享类型定义
└── package.json     # 工作区配置
```

## 快速开始

### 环境要求

- Node.js >= 18.0.0
- npm

### 安装依赖

```bash
npm install
```

### 启动开发服务器

启动后端服务（默认端口 3000）：

```bash
npm run dev:server
```

启动前端开发服务器（新终端窗口）：

```bash
npm run dev:client
```

然后访问 http://localhost:5173 开始游戏。

### 构建生产版本

```bash
npm run build:client
npm run build:server
```

### 运行测试

```bash
# 运行所有测试
npm test

# 仅测试客户端
npm run test:client

# 仅测试服务端
npm run test:server
```

## 游戏玩法

1. 选择语言模式（中文成语/英文单词）
2. 选择难度等级
3. 点击两张匹配的卡片（词语与释义）
4. 两张卡片需要能通过不超过 2 个拐点的路径连接
5. 消除所有卡片即可通关

## API 接口

### 获取词汇数据

```
POST /api/words/generate

请求体：
{
  "language": "zh" | "en",
  "level": "easy" | "medium" | "hard",
  "theme": "learning",
  "count": 8
}
```

## License

MIT
